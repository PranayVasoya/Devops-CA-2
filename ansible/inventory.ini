# This group is for your application servers.
[servers]

# Replace "your_server_ip" with the public IP address of your server.
# Replace "ubuntu" with the server's SSH username (e.g., ec2-user, root).
# Ensure your local SSH public key is in the server's ~/.ssh/authorized_keys file for passwordless login.
your_server_ip ansible_user=ubuntu